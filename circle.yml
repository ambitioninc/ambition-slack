machine:
    environment:
        SLACK_API_TOKEN: __default__

dependencies:
    override:
        - ./venv/bin/pip install -r requirements.txt

test:
    override:
        - ./venv/bin/python manage.py test
